# 开发环境专用（vite dev / pnpm dev）
# 注意：所有 VITE_ 变量会注入前端代码，请勿放置密钥。

# 后端 API 地址（可选）
# 说明：在开发环境（vite dev）本项目采用“同源 + Vite proxy”的方式（方案A），
# 前端会忽略 VITE_API_BASE_URL，所有 /api/... 请求将由 Vite 代理转发到后端。
# 你可以保留该值（不会生效），也可以置空以减少困惑。
VITE_API_BASE_URL=http://localhost:8080
