# 预览开关（会被注入前端代码，请勿放置密钥）：
# - true  => 全站只展示“网站开发中”提示页
# - false => 正常展示站点内容
VITE_PREVIEW=false

# 默认语言（zh / en）
VITE_LOCALE=zh

# 后端 API 地址：
# - 开发环境：在 .env.development 中配置（默认 http://localhost:8080）
# - 生产环境：在 .env.production 中留空（由 nginx 的 /api 反代到后端）
#
# VITE_API_BASE_URL=

# 上传图片最大体积（字节）。默认 1MB。
VITE_MAX_IMAGE_UPLOAD_BYTES=1048576
